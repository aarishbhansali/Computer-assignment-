#include <stdio.h>

int main()
{
    int x, y;
    printf("Enter number x: ");
    scanf("%d", &x);

    printf("Enter number y: ");
    scanf("%d", &y);

    printf("Addition of two numbers is %d\n", x + y);

    return 0;
}