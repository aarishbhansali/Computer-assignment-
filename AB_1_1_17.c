#include<stdio.h>
int main()
{
float length;
printf("enter the length of side of the square is ");
scanf("%f",&length);
printf("area of square is %f\n",length*length);
printf("perimeter of square is %f\n" , 4.00*length);
return 0;
}